{
  "name": "cut",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A quiet 3D slicing sandbox — name an object, it gets built, then cut it apart.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node tools/test-slicer.mjs && node tools/test-forge.mjs && node tools/test-physics.mjs && node tools/test-cuts.mjs",
    "bench": "node tools/bench-cut.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
